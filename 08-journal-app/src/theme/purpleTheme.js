import { createTheme } from "@mui/material";

export const purpleTheme = createTheme({
  palette: {
    primary: {
      main: "#9c27b0",
      contrastText: "#fff",
    },
    secondary: {
      main: "#ff4081",
    },
    error: {
      main: "#f44336",
    },
  },
});
